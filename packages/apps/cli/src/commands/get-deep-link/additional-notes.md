Examples:

```json
{
  "resource": {
    "type": "document",
    "collectionId": "Collection_...",
    "documentId": "Document_..."
  }
}
```

```json
{
  "resource": {
    "type": "app",
    "appId": "App_..."
  }
}
```

Use the returned `deepLink` in external notes, coding-agent responses, or other
places that should open the Superego desktop app.
