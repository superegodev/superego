import { style } from "@vanilla-extract/css";

export const DocumentsTable = {
  remoteUrlLink: style({
    width: "100%",
    height: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }),
};
