import { style } from "@vanilla-extract/css";

export const CreateApp = {
  panelContent: style({
    minHeight: 0,
  }),
};

export const CreateAppForm = {
  root: style({
    height: "100%",
  }),
};
