import { style } from "@vanilla-extract/css";
import { vars } from "../../../themes.css.js";

export const CreateCollectionManual = {
  assistedModeIcon: style({
    height: vars.spacing._5,
    width: vars.spacing._5,
  }),
};
