const schema = "/CollectionSchema.ts" as const;
const currentSchema = "/CurrentCollectionSchema.ts" as const;
const nextSchema = "/NextCollectionSchema.ts" as const;

export default { schema, currentSchema, nextSchema };
