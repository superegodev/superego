{
  "name": "@superego/browser-app-e2e-tests",
  "version": "2026.5.4",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test:check-scenarios": "node scripts/checkStepsAndSnapshots.ts",
    "test:playwright": "playwright test --config=playwright.config.ts",
    "test": "yarn test:check-scenarios && yarn test:playwright",
    "playwright": "playwright"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "@superego/backend": "workspace:*",
    "@superego/browser-app": "workspace:*",
    "@superego/schema": "workspace:*",
    "luxon": "^3.7.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
